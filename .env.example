# CodeVideoCanvas — 环境变量示例
# 复制为 .env.local 后按需修改；以下均为可选项。
#
# 注意：StepFun（阶跃星辰）API Key 不在此处配置。
# Key 由应用「设置」页写入本地 SQLite 的 settings 表，仅存服务端、永不进前端 bundle。

# 本地数据目录（SQLite 数据库 + 渲染产物）；默认 ./.data
# DATA_DIR=./.data

# StepFun OpenAI 兼容端点；默认 https://api.stepfun.com/v1
# STEPFUN_BASE_URL=https://api.stepfun.com/v1
