开发启动：`pnpm install && pnpm dev`（Node ≥22.19.0）。数据库迁移：`pnpm db:generate` 生成 Drizzle 迁移，`pnpm db:migrate` 执行迁移脚本。测试：`pnpm test`（Vitest run）或 `pnpm test:watch`。类型检查：`pnpm typecheck`。构建：`pnpm build`。lint：`pnpm lint`。v3 本地 Postgres/Trigger/HyperFrames 命令将在对应 N1/N2/N4 Track 落地后成为正式入口。