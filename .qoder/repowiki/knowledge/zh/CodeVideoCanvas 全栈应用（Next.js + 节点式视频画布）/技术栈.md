Next.js ≥16.2 (App Router) + React ≥19.2 + TypeScript strict；画布用 `@xyflow/react`，动画用 GSAP（paused + frame seek 保证确定性）；渲染走 HyperFrames 思想——Playwright/Chromium 逐帧截图后 `ffmpeg-static` 编码 MP4；Agent 使用 `@earendil-works/pi-agent-core` + `pi-ai`（OpenAI 兼容 StepFun Key）；存储 `better-sqlite3` + `drizzle-orm`；样式 Tailwind CSS v4 + Design Token 体系；包管理 pnpm@9。