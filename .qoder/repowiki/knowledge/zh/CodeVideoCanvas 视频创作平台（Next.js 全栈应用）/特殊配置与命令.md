依赖 pnpm@9.15.0 安装；原生依赖 `better-sqlite3`、`esbuild`、`ffmpeg-static` 需通过 `pnpm overrides.onlyBuiltDependencies` 显式允许编译。数据库初始化：`pnpm db:generate` 生成迁移，`pnpm db:migrate` 执行 `scripts/setup/db-migrate.ts`。开发：`pnpm dev`；构建：`pnpm build`；测试：`pnpm test` / `pnpm test:watch`；类型检查：`pnpm typecheck`。