- 严格模式 + noEmit，使用 bundler 解析与 `@/*` 路径别名，禁止 any，用 unknown + 收窄替代
- 默认 Server Component，'use client' 下沉到叶子组件，重型客户端组件通过 next/dynamic 懒加载
- 样式全部走 Tailwind + clsx/cn，颜色/阴影/间距必须引用设计系统 Token，禁止硬编码 hex/rgba
- 图标白名单制，仅使用 lucide-react 中已登记图标，禁用 emoji
- UI 视觉原语唯一来源 components/ui，页面 import 复用，新组件必须在 /playbook registry 登记并附 demo
- 调试原型放 src/app/_dev/，正式代码单向不可反向引用 _dev