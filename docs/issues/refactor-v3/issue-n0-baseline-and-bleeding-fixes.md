# Track N0 Baseline Closure and Bleeding Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 冻结可复核的 Demo v1 基线，修掉会污染 v3 迁移结论的 Pi 产物丢失、过晚 render 合同检查、API 结果丢弃与假状态，并建立只阻止新增架构债的自动门禁。

**Architecture:** N0 不迁移数据库、队列、模型路由或 renderer；它在现有 SQLite 管线外建立版本/证据锚点，并把错误尽量前移到产生副作用之前。Pi 仍是唯一 Agent Runtime，结构化业务产物优先来自已成功 Tool call 的参数；普通 `openai` 包是当前 StepFun/Gemini 兼容客户端，不等于 OpenAI Agents SDK，N0 禁止的是 `@openai/agents*`，不得误删 `openai`。

**Tech Stack:** Next.js 16、TypeScript strict、Vitest、Pi Agent 0.81.1、Playwright、现有 SQLite/Drizzle（仅 N0 期间）、PowerShell、pnpm 9

---

## 规范与施工边界

- 需求：`PROD-FOUND-003..004`、`PROD-AI-005`、`PROD-RENDER-002`、`PROD-RUN-006`、`PROD-UI-001..004`。
- 架构：`ARCH-DEC-003..004`、`ARCH-MOD-004..005`、`CONTRACT-AI-005..006`、`SEC-003`、`TEST-004`。
- 本 Track 不引入 Postgres、Trigger.dev、HyperFrames CLI、`AiTaskKind` 或新 UI 视觉原语；这些分别由 N1、N2、N3、N4、N6 接管。
- 每个 Task 开始前先执行 Harness 的 Git preflight；只 stage 本 Task 文件，不 push。
- 本 Issue 的 checkbox 是执行步骤，不是第二份 Task 状态账本；状态只改
  `docs/specs/2026-07-24-refactor-v3-task-breakdown.md`。

## 文件职责图

| 路径 | 单一职责 |
|---|---|
| `src/lib/workflow/version.ts` | 当前可执行管线的版本 pins；N2 fingerprint 直接消费 |
| `scripts/verify/capture-v3-baseline.ts` | 执行基线命令并生成脱敏证据 |
| `src/features/director/pi-output.ts` | 从 Pi transcript 中按显式 policy 提取业务产物 |
| `src/features/render/source-contract.ts` | render source 确定性静态门禁 |
| `src/features/render/admission.ts` | 入队前验证已提交 source 与 `__CVC_RENDER__@v1` |
| `src/app/(app)/canvas/pipeline-feedback.ts` | 把真实 API 返回值归一为 UI 文案 |
| `scripts/verify/v3-architecture.ts` | import、文件长度、UTF-8 和禁用 SDK 的增量门禁 |
| `scripts/verify/v3-architecture-baseline.json` | 仅记录 N0 开工时已存在的债务上限 |

<a id="task-n01"></a>

### Task N0.1: 冻结 Demo v1、建立 workflowVersion 与可复核基线

**Dependencies:** 无；必须先完成。

**Files:**

- Create: `src/lib/workflow/version.ts`
- Create: `src/lib/workflow/version.test.ts`
- Create: `src/lib/workflow/baseline-redaction.test.ts`
- Create: `scripts/verify/capture-v3-baseline.ts`
- Create (generated by the script): `docs/evidence/refactor-v3/n0-baseline.json`
- Modify: `eslint.config.mjs`（只排除仓库已忽略的 `tmp/**` 用户临时区）
- Read-only verification: `package.json`
- Read-only verification: `pnpm-lock.yaml`
- Read-only verification: `docs/specs/README.md`
- Read-only verification: `docs/specs/2026-07-23-prd-code-video-canvas.md`
- Read-only verification: `docs/specs/2026-07-23-ai-development-harness.md`
- Read-only verification: `docs/specs/2026-07-23-harness-task-breakdown.md`
- Read-only verification: `docs/designs/tasks.md`
- Read-only verification: `docs/issues/known-issues.md`
- Prohibited: 修改上述 package/lockfile 与六份只读文档；Pi production dependency 迁移只由 N3.6 执行
- Prohibited: `src/lib/db/**`
- Prohibited: `docs/designs/canvas.pen`
- Prohibited: 读取、修改、删除或 stage `tmp/**`
- Prohibited: `.env`, `.env.local` 与任何 Key 原文

- [ ] **Step 1: 记录 N0 preflight 并确认旧账本尚未被本 Track 修改**

Run:

```powershell
git status --short --branch
git log -5 --oneline
git diff --stat
git diff --cached --stat
node --version
pnpm --version
```

Expected: Node 主版本至少为 22，pnpm 为 9.x；输出中现有修改逐项归属清楚，cached diff 为空或只含执行者明确拥有的文件。

- [ ] **Step 1A: 隔离被 Git 忽略的用户临时区**

若完整 `pnpm lint` 仅因 `.gitignore` 已排除的 `tmp/**` 失败，只允许在
`eslint.config.mjs` 的 `ignores` 中加入 `tmp/**`，随后重新运行 `pnpm lint`。
不得读取、修改、删除或 stage 临时区内容；该配置修正用于让仓库门禁只检查版本控制
范围，不得扩展成清理用户文件。

- [ ] **Step 2: 写 workflowVersion 的失败测试**

在 `src/lib/workflow/version.test.ts` 写：

```ts
import { describe, expect, it } from 'vitest'
import { ACTIVE_WORKFLOW_VERSION, serializeWorkflowVersion } from './version'

describe('ACTIVE_WORKFLOW_VERSION', () => {
  it('诚实描述 N0 仍在运行的 legacy compiler/seek 管线', () => {
    expect(ACTIVE_WORKFLOW_VERSION).toEqual({
      workflow: 'cvc-v3-foundation',
      contracts: 'cvc-arch-v3.0.0',
      compiler: 'legacy-html-v1',
      hyperframes: 'legacy-cvc-render-v1',
      renderImage: 'node22-playwright1.61.1-ffmpeg-static5.3.0',
    })
  })

  it('使用固定字段顺序序列化，供 N2 fingerprint 消费', () => {
    expect(serializeWorkflowVersion(ACTIVE_WORKFLOW_VERSION)).toBe(
      'cvc-v3-foundation|cvc-arch-v3.0.0|legacy-html-v1|' +
        'legacy-cvc-render-v1|node22-playwright1.61.1-ffmpeg-static5.3.0'
    )
  })
})
```

- [ ] **Step 3: 运行测试，确认 RED**

Run: `pnpm test -- src/lib/workflow/version.test.ts`

Expected: FAIL，错误包含 `Cannot find module './version'`。

- [ ] **Step 4: 实现当前版本 pins**

在 `src/lib/workflow/version.ts` 写：

```ts
export interface WorkflowVersionV1 {
  workflow: string
  contracts: string
  compiler: string
  hyperframes: string
  renderImage: string
}

export const ACTIVE_WORKFLOW_VERSION = Object.freeze({
  workflow: 'cvc-v3-foundation',
  contracts: 'cvc-arch-v3.0.0',
  compiler: 'legacy-html-v1',
  hyperframes: 'legacy-cvc-render-v1',
  renderImage: 'node22-playwright1.61.1-ffmpeg-static5.3.0',
} satisfies WorkflowVersionV1)

export function serializeWorkflowVersion(version: WorkflowVersionV1): string {
  return [
    version.workflow,
    version.contracts,
    version.compiler,
    version.hyperframes,
    version.renderImage,
  ].join('|')
}
```

这里的 legacy pins 是事实陈述，不是假装 N4 已完成；N4 切换 compiler/HyperFrames 时必须改 pins 并使缓存失效。

- [ ] **Step 5: 只读验证活动入口、冻结 banner 与依赖现状**

Run:

```powershell
rg -n "CVC-PRODUCT-V3|CVC-ARCH-V3|CVC-HARNESS-V3|CVC-TASKS-V3" docs/specs/README.md
rg -n "Superseded / Frozen Demo v1|Archived Demo v1 trace view|Frozen Demo v1 issue index" docs/specs/2026-07-23-prd-code-video-canvas.md docs/specs/2026-07-23-ai-development-harness.md docs/specs/2026-07-23-harness-task-breakdown.md docs/designs/tasks.md docs/issues/known-issues.md
git diff --exit-code -- docs/specs/README.md docs/specs/2026-07-23-prd-code-video-canvas.md docs/specs/2026-07-23-ai-development-harness.md docs/specs/2026-07-23-harness-task-breakdown.md docs/designs/tasks.md docs/issues/known-issues.md package.json pnpm-lock.yaml
pnpm why '@earendil-works/pi-agent-core'
pnpm why '@earendil-works/pi-ai'
pnpm why openai
```

Expected: 活动入口同时列出四份 v3 权威文档；五份旧文档各自已有冻结/归档 banner 和指向 v3 的链接；`git diff --exit-code` 为 0，证明 N0 没有重复编辑已冻结账本；Pi 两个包仍按 N0 开工时的实际位置报告，迁移登记给 N3.6；普通 `openai` 仍存在且不被误当成 Agents SDK。

- [ ] **Step 6: 实现基线证据生成器**

`scripts/verify/capture-v3-baseline.ts` 必须固定执行 `git rev-parse --abbrev-ref HEAD`、
`git rev-parse HEAD`、`node --version`、`pnpm --version`、`pnpm lint`、
`pnpm typecheck`、`pnpm test`、`pnpm build`，并写出：

```ts
interface BaselineEvidenceV1 {
  schemaVersion: 1
  capturedAt: string
  branch: string
  commit: string
  workflowVersion: typeof ACTIVE_WORKFLOW_VERSION
  commands: readonly {
    command: string
    exitCode: number
    durationMs: number
    stdoutSha256: string
    lastLines: readonly string[]
  }[]
}
```

实现约束：使用 `spawnSync` 的参数数组，Windows 调 pnpm 时使用 `pnpm.cmd`；每项
`lastLines` 最多 20 行、每行最多 500 字符；不读取 `.env*`；任一 gate 非 0 时仍先写证据，再令脚本退出 1。
`lastLines` 还必须通过对抗测试证明不会持久化 Windows/UNC 绝对路径、Basic/Bearer
Authorization、Cookie、DSN/connection string、URI secret 参数、私钥块、继承环境
secret 或长高熵 token。

- [ ] **Step 7: 运行 GREEN 与基线捕获**

Run:

```powershell
pnpm test -- src/lib/workflow/version.test.ts src/lib/workflow/baseline-redaction.test.ts
pnpm tsx scripts/verify/capture-v3-baseline.ts
```

Expected: workflow 测试 PASS；第二条生成 `docs/evidence/refactor-v3/n0-baseline.json`。若旧基线本身失败，JSON 必须准确记录非 0，执行者按 Harness 登记阻塞，不得在 N0.1 越界修无关代码。

- [ ] **Step 8: Task-Light 检查并本地提交**

Run:

```powershell
pnpm eslint src/lib/workflow/version.ts src/lib/workflow/version.test.ts src/lib/workflow/baseline-redaction.test.ts scripts/verify/capture-v3-baseline.ts eslint.config.mjs
git diff --check
rg -n --fixed-strings ([string][char]0xFFFD) AGENTS.md README.md docs src scripts
git add -- src/lib/workflow/version.ts src/lib/workflow/version.test.ts src/lib/workflow/baseline-redaction.test.ts scripts/verify/capture-v3-baseline.ts docs/evidence/refactor-v3/n0-baseline.json eslint.config.mjs
git diff --cached --check
git commit -m "chore(refactor): freeze demo baseline and pin workflow version"
```

Expected: lint、diff、U+FFFD scan 均通过；commit 只含列出的路径。

**N0.1 exit gate:** v3 活动入口与旧文档冻结 banner 已只读验证且零重复编辑；当前管线 pins 诚实可序列化；Pi 依赖迁移明确留给 N3.6；普通 `openai` 未被误删；四项完整基线有机器可读证据。

<a id="task-n02"></a>

### Task N0.2: 让 Pi Tool 参数成为业务产物，不再只读最终 assistant 文本

**Dependencies:** N0.1。

**Files:**

- Create: `src/features/director/pi-output.ts`
- Create: `src/features/director/pi-output.test.ts`
- Modify: `src/features/director/pi-session.ts`
- Modify: `src/features/director/pi-session.test.ts`
- Modify: `src/features/director/stage-runner.ts`
- Modify: `src/features/director/stage-runner.test.ts`
- Modify: `src/features/director/tools/validate-shot-plan.ts`
- Modify: `src/features/director/tools/validate-shot-plan.test.ts`
- Modify: `src/features/director/tools/check-determinism.ts`
- Modify: `src/features/director/tools/check-determinism.test.ts`
- Prohibited: 新增第二套 Agent/SDK、`@openai/agents*`
- Prohibited: 保存 hidden reasoning/thinking 内容
- Prohibited: 改 prompt/schema 的业务含义

- [ ] **Step 1: 写录制 transcript 的失败测试**

`src/features/director/pi-output.test.ts` 至少覆盖：

```ts
const transcript = [
  {
    role: 'assistant',
    content: [{
      type: 'toolCall',
      id: 'call-1',
      name: 'validate_shot_plan',
      arguments: { shotPlan: { schemaVersion: 1, shots: [] } },
    }],
  },
  {
    role: 'toolResult',
    toolCallId: 'call-1',
    toolName: 'validate_shot_plan',
    content: [{ type: 'text', text: '{"ok":true}' }],
    details: { ok: true, shotCount: 0 },
    isError: false,
  },
  {
    role: 'assistant',
    content: [{ type: 'text', text: '完成' }],
  },
] as AgentMessage[]

expect(extractDirectorOutput(transcript, {
  kind: 'validated-tool-argument',
  toolName: 'validate_shot_plan',
  argumentKey: 'shotPlan',
})).toMatchObject({
  artifactContent: '{"schemaVersion":1,"shots":[]}',
  displayText: '完成',
  provenance: {
    kind: 'tool-argument',
    toolName: 'validate_shot_plan',
    toolCallId: 'call-1',
  },
})
```

还必须测试：`isError: true`、`details.ok: false`、Tool name 不匹配时显式失败；policy 为
`assistant-text` 时返回最后一个非空 assistant 文本；thinking block 永不进入
`artifactContent` 或 `displayText`。

- [ ] **Step 2: 运行 RED**

Run: `pnpm test -- src/features/director/pi-output.test.ts`

Expected: FAIL，错误包含 `Cannot find module './pi-output'`。

- [ ] **Step 3: 实现显式输出 policy 与 provenance**

`src/features/director/pi-output.ts` 的公开合同固定为：

```ts
export type DirectorOutputPolicy =
  | { kind: 'assistant-text' }
  | {
      kind: 'validated-tool-argument'
      toolName: string
      argumentKey: string
    }

export interface DirectorOutput {
  artifactContent: string
  displayText: string
  provenance:
    | { kind: 'assistant-text'; timestamp?: number }
    | { kind: 'tool-argument'; toolName: string; toolCallId: string }
}

export function extractDirectorOutput(
  messages: readonly AgentMessage[],
  policy: DirectorOutputPolicy
): DirectorOutput
```

Tool policy 的算法必须是：从后向前找 `role === 'toolResult'`、name 匹配、
`isError === false`、`details` 为对象且 `details.ok === true`；再按 `toolCallId`
定位 assistant 的 `toolCall`；仅取指定 argument。argument 是 string 时原样返回，
其他 JSON 值序列化；缺失或不可序列化时抛出
错误 code 固定为 `DIRECTOR_TOOL_OUTPUT_MISSING`，消息包含 policy 中的真实
`toolName`。禁止“Tool 失败后偷偷回退 assistant 文本”。

- [ ] **Step 4: 把 policy 接入 session 与 stage runner**

把 `DirectorRunInput`/`DirectorRunResult` 改为：

```ts
export interface DirectorRunInput {
  prompt: string
  tools?: readonly DirectorTool[]
  output: DirectorOutputPolicy
}

export interface DirectorRunResult extends DirectorOutput {}
```

`DirectorSession.run()` 在 `waitForIdle()` 后调用 `extractDirectorOutput()`。阶段映射必须显式为：

```ts
const STAGE_OUTPUT: Record<PipelineStage, DirectorOutputPolicy> = {
  INGEST: { kind: 'assistant-text' },
  DIRECT: { kind: 'assistant-text' },
  SHOT_SPEC: {
    kind: 'validated-tool-argument',
    toolName: 'validate_shot_plan',
    argumentKey: 'shotPlan',
  },
  FABRICATE: {
    kind: 'validated-tool-argument',
    toolName: 'check_determinism',
    argumentKey: 'source',
  },
  ASSEMBLE: { kind: 'assistant-text' },
  FINALIZE: { kind: 'assistant-text' },
}
```

`prepareResult()`/artifact 写入只消费 `artifactContent`；可回看日志只消费
`displayText`。这两条通道不得再共用一个 `text` 字段。

- [ ] **Step 5: 成功验证 Tool 必须终止本轮**

`validate_shot_plan` 与 `check_determinism` 的成功返回加 `terminate: true`；失败仍为
`terminate: false`。测试分别断言成功/失败，防止错误产物提前终止。

- [ ] **Step 6: 运行 GREEN 与 focused regression**

Run:

```powershell
pnpm test -- src/features/director/pi-output.test.ts src/features/director/pi-session.test.ts src/features/director/stage-runner.test.ts src/features/director/tools/validate-shot-plan.test.ts src/features/director/tools/check-determinism.test.ts
pnpm typecheck
```

Expected: 全部 PASS；TypeScript 不含 `any` 新增；录制 transcript 中 trailing “完成”不能覆盖 Tool 参数。

- [ ] **Step 7: 扫描唯一 Agent Runtime 并提交**

Run:

```powershell
rg -n "from ['\"]@openai/agents|from ['\"]@openai/agents-" src scripts package.json
rg -n "new Agent\\(" src
git diff --check
git add -- src/features/director/pi-output.ts src/features/director/pi-output.test.ts src/features/director/pi-session.ts src/features/director/pi-session.test.ts src/features/director/stage-runner.ts src/features/director/stage-runner.test.ts src/features/director/tools/validate-shot-plan.ts src/features/director/tools/validate-shot-plan.test.ts src/features/director/tools/check-determinism.ts src/features/director/tools/check-determinism.test.ts
git diff --cached --check
git commit -m "fix(director): preserve validated Pi tool artifacts"
```

Expected: `@openai/agents*` scan 无匹配；`new Agent(` 只在 Pi 封装中；commit 成功。

**N0.2 exit gate:** SHOT_SPEC/FABRICATE 业务 artifact 可由录制 Pi transcript 精确复原；失败 Tool 不会提交；assistant 展示文本与业务产物已分离。

<a id="task-n03"></a>

### Task N0.3: 把 source/runtime 合同门禁前移到 render 入队之前

**Dependencies:** N0.2。

**Files:**

- Create: `src/features/render/source-contract.ts`
- Create: `src/features/render/source-contract.test.ts`
- Create: `src/features/render/admission.ts`
- Create: `src/features/render/admission.test.ts`
- Modify: `src/features/render/repository.ts`
- Modify: `src/features/render/repository.test.ts`
- Modify: `src/features/render/renderer.ts`
- Modify: `src/features/render/queue-handler.ts`
- Modify: `src/features/render/queue-handler.test.ts`
- Modify: `src/features/director/advance.ts`
- Modify: `src/features/director/advance.test.ts`
- Modify: `src/app/api/render/route.ts`
- Modify: `src/app/api/render/route.test.ts`
- Prohibited: 在 render 失败时调用 `fabricateShot`
- Prohibited: 入队失败后留下 `pending`
- Prohibited: 绕过 `StorageAdapter`

- [ ] **Step 1: 写三个 RED 行为测试**

测试必须证明：

1. `requestAnimationFrame` source 在 `queue.enqueue` 前失败；
2. 缺少/版本错误的 `window.__CVC_RENDER__` 在 `pending` 前失败；
3. handler 找不到已提交 source 时直接失败，不反向调用 Director。

`admission.test.ts` 使用依赖注入的 storage/open session，不启动真实浏览器：

```ts
await expect(assertRenderAdmission(renderJob, {
  storage: storageOf('requestAnimationFrame(render)'),
  openFrameCapture: vi.fn(),
})).rejects.toThrow('确定性违规')
expect(openFrameCapture).not.toHaveBeenCalled()
```

`queue-handler.test.ts` 断言拒绝时 `transitionNodeStatus` 与 `queue.enqueue` 都未调用。

- [ ] **Step 2: 运行 RED**

Run:

```powershell
pnpm test -- src/features/render/source-contract.test.ts src/features/render/admission.test.ts src/features/render/queue-handler.test.ts
```

Expected: FAIL，至少包含缺失模块/缺失 `assertRenderAdmission`。

- [ ] **Step 3: 抽出唯一静态 source 门禁**

`source-contract.ts` 只导出：

```ts
export function assertDeterministicSource(source: string): void {
  const violations = checkSource(source)
  if (violations.length === 0) return
  const summary = violations
    .map(({ ruleId, line }) => `${ruleId}@${line}`)
    .join(', ')
  throw new Error(`确定性违规：${summary}`)
}
```

`renderer.ts` 改为复用该函数，删除本地重复实现。

- [ ] **Step 4: 实现入队前 runtime admission**

`admission.ts` 的顺序固定为：

```ts
export async function assertRenderAdmission(
  job: RenderJob,
  dependencies: RenderAdmissionDependencies
): Promise<void> {
  const html = await dependencies.storage.get(job.htmlKey)
  assertDeterministicSource(html.toString('utf8'))
  const session = await dependencies.openFrameCapture(
    dependencies.storage.localPath(job.htmlKey),
    { width: job.frames.width, height: job.frames.height }
  )
  await session.close()
}
```

`openFrameCapture()` 已验证 runtime 存在、version 为 1、seek 为函数；`close()` 必须在
`finally`，即使后续 admission 扩展失败也不泄露 browser。

- [ ] **Step 5: 分开 admission context 与 running context**

`RenderRepository` 新增 `loadRenderAdmissionContext(projectId,nodeId)`：验证项目归属、
`shot-codegen`、enqueueable status、laneKey、renderSpec、最新
`director-fabricate` artifact，并返回完整 `RenderJob`。`loadRenderContext()` 只增加
`status === 'running'` 条件后复用同一私有构造函数，避免两份 artifact 查询。

- [ ] **Step 6: 让 enqueue 异步并删除 render→fabricate fallback**

`enqueueRenderShot()` 必须按以下顺序：

```ts
const payload = renderJobPayloadSchema.parse(input)
const job = await dependencies.loadAdmissionContext(payload.projectId, payload.nodeId)
await dependencies.assertAdmission(job)
dependencies.transitionNodeStatus(payload.nodeId, 'pending')
return dependencies.queue.enqueue('render-shot', payload, {
  projectId: payload.projectId,
  nodeId: payload.nodeId,
})
```

同步更新 API 和 `advancePipeline()` 的 `await`。handler 删除
`fabricateShot()` fallback：没有 source 是当前 task 的确定失败，不能隐式产生一次模型调用。

- [ ] **Step 7: 运行 GREEN 与真实 runtime fixture**

Run:

```powershell
pnpm test -- src/features/render/source-contract.test.ts src/features/render/admission.test.ts src/features/render/queue-handler.test.ts src/features/render/repository.test.ts src/features/director/advance.test.ts src/app/api/render/route.test.ts
pnpm test -- src/features/render/renderer.integration.test.ts
pnpm typecheck
rg -n "fabricateShot|features/director/fabricate" src/features/render
```

Expected: 测试 PASS；typecheck 退出 0；最后一条无匹配。真实 fixture 证明的边界是
`__CVC_RENDER__@v1` admission，不是 N4 HyperFrames。

- [ ] **Step 8: 提交**

```powershell
git add -- src/features/render/source-contract.ts src/features/render/source-contract.test.ts src/features/render/admission.ts src/features/render/admission.test.ts src/features/render/repository.ts src/features/render/repository.test.ts src/features/render/renderer.ts src/features/render/queue-handler.ts src/features/render/queue-handler.test.ts src/features/director/advance.ts src/features/director/advance.test.ts src/app/api/render/route.ts src/app/api/render/route.test.ts
git diff --cached --check
git commit -m "fix(render): validate source runtime before enqueue"
```

**N0.3 exit gate:** 非确定性、缺 artifact、无效 runtime 都在 `pending`/queue 副作用前失败；Render 不再在失败路径触发 FABRICATE。

<a id="task-n04"></a>

### Task N0.4: 保留 API 结果并移除迁移验收中的假状态

**Dependencies:** N0.3。

**Files:**

- Create: `src/app/(app)/canvas/pipeline-feedback.ts`
- Create: `src/app/(app)/canvas/pipeline-feedback.test.ts`
- Modify: `src/app/(app)/canvas/canvas-view.tsx`
- Modify: `src/app/(app)/canvas/canvas-inspector.tsx`
- Modify: `src/app/(app)/canvas/canvas-action-api.ts`
- Modify: `src/app/(app)/canvas/canvas-action-api.test.ts`
- Modify: `src/components/ui/queue-status-bar.tsx`
- Modify: `src/components/ui/queue-status-bar.demo.tsx`
- Create: `src/components/ui/queue-status-bar.test.ts`
- Prohibited: 固定 cache hit、百分比、时间、分辨率或成功检测
- Prohibited: 把 local optimistic state 伪装成持久业务终态
- Prohibited: 修改 Pencil 视觉结构

- [ ] **Step 1: 写 API feedback 与状态条 RED 测试**

`pipeline-feedback.test.ts` 必须断言：

```ts
expect(describePipelineResult({
  autopilot: true,
  enqueuedNodeIds: ['n1', 'n2'],
  failedNodeIds: ['n3'],
})).toEqual({
  variant: 'error',
  title: '工作流已启动，但有节点入队失败',
  body: '已入队 2 个节点，失败 1 个节点。',
})
```

`queue-status-bar.test.ts` 必须断言 `describeQueueActivity()` 只由
`completed/active/failed/total` 派生，并扫描组件源码不含 `命中缓存 5 次`。

- [ ] **Step 2: 运行 RED**

Run:

```powershell
pnpm test -- "src/app/(app)/canvas/pipeline-feedback.test.ts" src/components/ui/queue-status-bar.test.ts
```

Expected: FAIL，缺少目标模块/导出。

- [ ] **Step 3: 实现纯 feedback 映射**

固定分支：

```ts
export function describePipelineResult(
  result: PipelineControlResult
): PipelineFeedback {
  const enqueued = result.enqueuedNodeIds?.length ?? 0
  const failed = result.failedNodeIds?.length ?? 0
  if (failed > 0) {
    return {
      variant: 'error',
      title: '工作流已启动，但有节点入队失败',
      body: `已入队 ${enqueued} 个节点，失败 ${failed} 个节点。`,
    }
  }
  return result.autopilot
    ? {
        variant: 'success',
        title: '工作流已启动',
        body: `已入队 ${enqueued} 个节点。`,
      }
    : {
        variant: 'success',
        title: '已停止自动推进',
        body: '已入队作业不会被伪装为已取消。',
      }
}
```

- [ ] **Step 4: Canvas 必须消费真实返回值**

`togglePipeline()` 保存 `await startPipeline()/stopPipeline()` 的返回值并显示
`describePipelineResult()`；`triggerNodeAction()` 返回的 `jobId` 传给
`onQueued(jobId)`，Inspector 显示“已入队”本地反馈但不显示“成功”。刷新后仍以服务端
节点状态为准。

- [ ] **Step 5: 移除 QueueStatusBar 假 cache 文案**

组件新增 `active`、`failed` 必填字段；右侧文案固定派生：

```ts
export function describeQueueActivity(input: QueueActivity): string {
  if (input.failed > 0) return `${input.failed} 个节点失败`
  if (input.active > 0) return `${input.active} 个节点执行中`
  if (input.total > 0 && input.completed === input.total) return '全部节点已完成'
  return '等待执行'
}
```

Loader 只在 `active > 0` 时旋转。Playbook demo 传入明确 demo 数据，正式 Canvas 用真实
node status 计数；demo 值不得流入正式路由。

- [ ] **Step 6: 运行 GREEN 与真实性扫描**

Run:

```powershell
pnpm test -- "src/app/(app)/canvas/pipeline-feedback.test.ts" "src/app/(app)/canvas/canvas-action-api.test.ts" src/components/ui/queue-status-bar.test.ts
pnpm typecheck
rg -n "命中缓存 5 次|62%|checked=\\{true\\}" "src/app/(app)" src/components/ui/queue-status-bar.tsx
```

Expected: tests/typecheck PASS；最后一条无匹配。

- [ ] **Step 7: 提交**

```powershell
git add -- "src/app/(app)/canvas/pipeline-feedback.ts" "src/app/(app)/canvas/pipeline-feedback.test.ts" "src/app/(app)/canvas/canvas-view.tsx" "src/app/(app)/canvas/canvas-inspector.tsx" "src/app/(app)/canvas/canvas-action-api.ts" "src/app/(app)/canvas/canvas-action-api.test.ts" src/components/ui/queue-status-bar.tsx src/components/ui/queue-status-bar.demo.tsx src/components/ui/queue-status-bar.test.ts
git diff --cached --check
git commit -m "fix(canvas): surface real pipeline results"
```

**N0.4 exit gate:** 一键启动的成功/部分失败不再被丢弃；单节点 jobId 有明确 optimistic 来源；正式状态条没有固定 cache hit 或假进度。

<a id="task-n05"></a>

### Task N0.5: 建立增量架构、文件长度与 UTF-8 门禁

**Dependencies:** N0.4。

**Files:**

- Create: `scripts/verify/v3-architecture.ts`
- Create: `src/lib/architecture/v3-architecture.test.ts`
- Create: `scripts/verify/v3-architecture-baseline.json`
- Modify: `package.json`
- Modify: `docs/evidence/refactor-v3/n0-baseline.json`（重新捕获最终 N0）
- Prohibited: 为让报告变绿而重构 N1–N6 范围代码
- Prohibited: 扫描/读取 `docs/designs/canvas.pen`
- Prohibited: 把 `openai` 误判为 `@openai/agents`

- [ ] **Step 1: 写门禁规则 RED 测试**

使用临时 fixture 验证：

- `@openai/agents`、`@openai/agents-core` package/import 必须失败；
- `import OpenAI from 'openai'` 在 N0–N2 不触发 Agents SDK 规则；
- `features/canvas` 新增到 Trigger/Pi/Drizzle/HyperFrames 的 import 必须失败；
- `trigger/tasks` 到 Drizzle/schema 的 import 必须失败；
- 新文件超过硬上限 350 行失败；
- baseline 文件行数只能下降或持平；
- Markdown/TS/TSX 出现 U+FFFD 失败；
- `.pen`、`.data`、`.next`、`node_modules` 不读取。

- [ ] **Step 2: 运行 RED**

Run: `pnpm test -- src/lib/architecture/v3-architecture.test.ts`

Expected: FAIL，缺少 `v3-architecture.ts`。

- [ ] **Step 3: 实现 check/report 两种模式**

CLI：

```text
pnpm tsx scripts/verify/v3-architecture.ts --write-baseline scripts/verify/v3-architecture-baseline.json
pnpm tsx scripts/verify/v3-architecture.ts --check
pnpm tsx scripts/verify/v3-architecture.ts --report
```

`--check` 对新违规退出 1；`--report` 输出 JSON 到 stdout、即使存在 baseline 债务也退出
0。`--write-baseline` 在目标文件不存在时扫描 N0 开工树并原子生成真实
`schemaVersion/debtCaps/oversizedFiles`；目标已存在时拒绝覆盖，只有先人工审阅并
删除该 generated file 才能重建。禁止在计划或实现中预填示例上限、手填较大值或
加入“忽略本次新增”的开关。规则采用精确
package 名：`@openai/agents` 及其 `/`、`-` 子包，普通 `openai` 单独归入
`directOpenAiClientImports`，删除期限是 N3。

- [ ] **Step 4: 添加 package scripts**

```json
{
  "verify:v3": "tsx scripts/verify/v3-architecture.ts --check",
  "report:v3": "tsx scripts/verify/v3-architecture.ts --report"
}
```

- [ ] **Step 5: 运行 GREEN 与 N0 最终 Tier B**

Run:

```powershell
pnpm test -- src/lib/architecture/v3-architecture.test.ts
if (-not (Test-Path -LiteralPath 'scripts/verify/v3-architecture-baseline.json')) {
  pnpm tsx scripts/verify/v3-architecture.ts --write-baseline scripts/verify/v3-architecture-baseline.json
}
pnpm verify:v3
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm tsx scripts/verify/capture-v3-baseline.ts
```

Expected: 全部退出 0；最终 baseline 四项命令均为 `exitCode: 0`。若现有用户 WIP 引发无关失败，按 Harness 记录，不扩大 N0 范围。

- [ ] **Step 6: 专项扫描并提交**

Run:

```powershell
rg -n --fixed-strings ([string][char]0xFFFD) AGENTS.md README.md docs src scripts
rg -n "from ['\"]@openai/agents|@openai/agents-" package.json pnpm-lock.yaml src --glob '!src/lib/architecture/v3-architecture.test.ts'
git diff --check
git add -- scripts/verify/v3-architecture.ts src/lib/architecture/v3-architecture.test.ts scripts/verify/v3-architecture-baseline.json package.json docs/evidence/refactor-v3/n0-baseline.json
git diff --cached --check
git commit -m "chore(architecture): enforce refactor v3 guardrails"
```

Expected: 两个 scan 均无匹配；commit 成功。

**N0.5 exit gate:** 新 Agents SDK、跨层 import、U+FFFD、文件超限回退会被自动阻止；历史债务有真实上限且不能增长。

## Track N0 退出门

- [ ] `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部退出 0。
- [ ] `pnpm verify:v3` 退出 0，U+FFFD 与 `@openai/agents*` scan 为 0。
- [ ] 录制 Pi transcript 证明 Tool args 胜过 trailing assistant 文本。
- [ ] `renderer.integration.test.ts` 证明旧 deterministic fixture 仍可走通；报告明确它不是 N4 HyperFrames 或真实 FABRICATE。
- [ ] render admission 失败发生在 node `pending` 与 queue enqueue 之前。
- [ ] 正式 Canvas 无固定 cache hit/假进度，API 部分失败可见。
- [ ] N0.1–N0.5 每项都有独立本地 Conventional Commit，未 push。
